# Synthetic evaluation corpus

These four documents are original, redistribution-safe fixtures created for this
repository. They contain no private course material. Regenerate them with:

```bash
python -m eval.build_fixtures
```

The corpus intentionally covers every supported file type and exact page, slide,
paragraph, and line-range locations.

